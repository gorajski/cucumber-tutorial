{
  "scripts": {
    "test": "npx mocha test/**/*.spec.js --timeout 60000 && cucumber-js",
    "cuke": "cucumber-js"
  },
  "dependencies": {
    "assert": "^2.0.0",
    "selenium-webdriver": "^4.4.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.6.0",
    "mocha": "^10.0.0"
  }
}
